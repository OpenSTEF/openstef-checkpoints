{
  "description": "Description of an exported checkpoint, written to a JSON file beside its weights.",
  "properties": {
    "schema_version": {
      "default": 2,
      "description": "Metadata layout version.",
      "title": "Schema Version",
      "type": "integer"
    },
    "model_family": {
      "description": "Model family identifier, e.g. 'chronos2'.",
      "title": "Model Family",
      "type": "string"
    },
    "input_names": {
      "description": "Model input tensor names, in order.",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Input Names",
      "type": "array"
    },
    "output_name": {
      "description": "Name of the quantile-prediction output tensor.",
      "title": "Output Name",
      "type": "string"
    },
    "native_quantiles": {
      "description": "Quantile levels the model emits, ascending.",
      "items": {
        "type": "number"
      },
      "minItems": 1,
      "title": "Native Quantiles",
      "type": "array"
    },
    "context_length": {
      "description": "Number of historical timesteps consumed as context.",
      "exclusiveMinimum": 0,
      "title": "Context Length",
      "type": "integer"
    },
    "output_patch_size": {
      "description": "Timesteps produced per output patch.",
      "exclusiveMinimum": 0,
      "title": "Output Patch Size",
      "type": "integer"
    },
    "horizon_patches": {
      "description": "Number of output patches emitted.",
      "exclusiveMinimum": 0,
      "title": "Horizon Patches",
      "type": "integer"
    },
    "resolution_minutes": {
      "description": "Expected sampling interval, in minutes.",
      "exclusiveMinimum": 0,
      "title": "Resolution Minutes",
      "type": "integer"
    },
    "precision": {
      "default": "fp32",
      "description": "Weight precision. int8 runs on CPU but not CoreML; fp16 and fp32 take the CoreML path.",
      "enum": [
        "fp32",
        "fp16",
        "int8"
      ],
      "title": "Precision",
      "type": "string"
    },
    "static_shapes": {
      "default": false,
      "description": "Whether every graph axis is a fixed size. Static graphs are eligible for CoreML.",
      "title": "Static Shapes",
      "type": "boolean"
    },
    "max_covariates": {
      "anyOf": [
        {
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Number of covariate series the graph is fixed to, or None if that axis is dynamic.",
      "title": "Max Covariates"
    }
  },
  "required": [
    "model_family",
    "input_names",
    "output_name",
    "native_quantiles",
    "context_length",
    "output_patch_size",
    "horizon_patches",
    "resolution_minutes"
  ],
  "title": "CheckpointMetadata",
  "type": "object"
}
